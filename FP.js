const delay = (ms, cb) => {
  // return a promise 
}

const createFile = (id, dir, delayMs, errRate, handleErr, sync, retry) => {
  // error rate ->0% || 30% ; sync -> boolean ; retry -> boolean
  //returns a promise
}

const startTask = (id, dir, numOfFiles, fileRecipe) => {
  //fileRecipt -> obj
  //returns a primise
}